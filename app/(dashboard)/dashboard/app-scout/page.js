import AppScoutClient from '@/components/app-scout/AppScoutClient';
export const metadata = { title: 'App Scout — AI Product OS' };
export default function AppScoutPage() {
  return <AppScoutClient />;
}
